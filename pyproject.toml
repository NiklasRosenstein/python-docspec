[project]
name = "python-docspec"
version = "2.2.1"
description = ""
readme = "readme.md"
requires-python = ">=3.8"
dependencies = []

[tool.uv.sources]
docspec = { workspace = true }
docspec-python = { workspace = true }

[tool.uv.workspace]
members = ["docspec/", "docspec-python/", "docs"]
exclude = ["docs"]
